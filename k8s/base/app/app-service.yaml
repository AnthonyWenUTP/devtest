apiVersion: v1
kind: Service
metadata:
  name: devops-service
spec:
  type: NodePort
  selector:
    app.kubernetes.io/instance: devops-playground
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30007